import { StyleSheet } from 'react-native';

const text = StyleSheet.create({
  title: {
    fontSize: 35,
    fontWeight: 'bold',
  },
  p: {
    color: 'brown',
    fontSize: 20,
  },
});

export default text;
